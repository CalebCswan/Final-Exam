package code

struct binary_tree_node
{
  binary_tree_node * left_child;
  binary_tree_node * right_child;
  value = 7
};
type BST struct {
	Value int
	Left  *BST
	Right *BST
}

func InsertIntoBST(root *BST, value int) *BST {
	if(root == NULL)
        return getNewNode(val);
	else if(root < right_child)
		return getNewNode(val);
	else if(root > right_child)
	return root
}

fmt.Println(InsertIntoBST)